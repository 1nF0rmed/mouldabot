# MouldaBot

SO-101 robotic arm simulation in MuJoCo with a table and colored blocks for manipulation tasks.
Uses a fine-tuned Ministral 8B model to orchestrate robot actions — translating high-level task instructions into low-level joint commands for pick-and-place operations.
Run `python sim.py` to launch the interactive viewer.
